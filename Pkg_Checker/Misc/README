This utility reports the (potential) defects in eReview packages.
Files:
-- ReviewPackageChecker.exe (main program)
-- itextsharp.dll           (depends lib)
-- README.txt

Ho to start from CLI and execute immediately:
Start without CM21 integration: thisApp reviewPackagePath resultPath
Start with CM21 integration: thisApp reviewPackagePath resultPath EID CM21Password SCRReportDownloadPath
Start with CM21 integration and a timeout value (in seconds): thisApp reviewPackagePath resultPath EID CM21Password SCRReportDownloadPath timeout


    
Known Issues:
*) Sometimes it will report a ***.TRT file not printed to the package, or missing SCR report, which actually did. This is a bug from the third-party lib.
